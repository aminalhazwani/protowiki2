<script setup>
definePage({
  meta: {
    title: 'Account creation v3 (alt)',
    description: 'Alternative variant of the v3 Wikipedia signup prototype with autogenerated usernames. Ctrl+Shift+S opens the admin panel; ?variant=v1 picks a variant.',
  },
})

import { ref, provide, onMounted, onUnmounted } from 'vue'
import CreateAccountView from './views/CreateAccountView.vue'
import AdminPanel from './components/AdminPanel.vue'
import './styles/global.css'

const showAdmin = ref(false)

function toggleAdmin() {
  showAdmin.value = !showAdmin.value
}

provide('toggleAdmin', toggleAdmin)

function onKeydown(e) {
  if (e.ctrlKey && e.shiftKey && e.key === 'S') {
    e.preventDefault()
    toggleAdmin()
  }
}

onMounted(() => window.addEventListener('keydown', onKeydown))
onUnmounted(() => window.removeEventListener('keydown', onKeydown))
</script>

<template>
  <CreateAccountView />
  <AdminPanel v-if="showAdmin" @close="showAdmin = false" />
</template>
